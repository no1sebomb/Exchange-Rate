# coding=utf-8

bind = "0.0.0.0:5000"  # Server address

workers = 3  # Number of server workers
timeout = 16  # Max workers timeout

# pidfile = "/tmp/exc_app.pid"  # Process ID file
# daemon = True  # UNIX Daemon process mode
